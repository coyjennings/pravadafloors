<div class="page-width">
  <header class="section-header text-center">
    <h1 class="section-header__title h2">{{ page.title }}</h1>
  </header>
  <div class="grid">
    <div class="grid__item medium-up--four-fifths medium-up--push-one-tenth wishlist-page">
      <div class="rte">
        {{ page.content }} <!-- include 'wishlist-collection' -->
      </div>
    </div>
  </div>
</div>
